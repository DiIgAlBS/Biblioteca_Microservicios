server:
  port: 8080 

spring:
  application:
    name: api-gateway 
  cloud:
    gateway:
      routes:
       
        - id: prestamos-service
          uri: lb://MICROSERVICIO3_PRESTAMOS 
          predicates:
            - Path=/api/prestamos/** 

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/ #